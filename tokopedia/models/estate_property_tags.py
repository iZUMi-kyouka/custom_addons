# from .... import models, fields

# class PropertyTag(models.Model):
#     _name = "estate.property.tag"
#     _description = "A tag to indicate that a property satisfies a particluar criterion."
    
#     name =  fields.Char('Tag', required=True)